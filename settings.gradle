pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // added jcenter() for Katex library
        jcenter()
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "MarksApp"
include ':app'
